declare and retain 
	ex as integer;

	/* check if a number equals 3 */
	isThree as function (a as integer): boolean
		declare local result as boolean;
		do
			set result to a = 3
			return result
		done

when your turn 
by default do 
	set x to 4 + isThree(6)
done